// if (!window.globalSocket) {
//   window.globalSocket = io();
//   //   console.log("Socket initialized " + window.socket.id);
// }
//  else {
//   console.log("Socket already exists " + window.socket.id);
// }

export const socket = io();
